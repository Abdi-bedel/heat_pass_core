name: heat_pass_core
description: Shared models, enums, and utils for Heat Pass apps.
version: 0.1.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  intl: ^0.19.0

dev_dependencies:
  build_runner: ^2.4.11
  freezed: ^2.5.2
  json_serializable: ^6.9.2
  test: ^1.25.2